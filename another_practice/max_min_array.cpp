#include <iostream>
using namespace std;

float input_taker (float max_min[] )
{
    float first_max, second_max, first_min, second_min, median;

    for (float i = 0; i < max_min[]; i++)
    {
        cin >> max_min[i];
    }

    if ( max_min[i] > first max && max_min[i] < second max )
    {
        cout << "1st max is: " << first_max <<endl;
        cout << "2nd max is: " << second_max <<endl;
    }

    if (max_min[i] > first min && max_min[i] < 2nd min)
    {
        cout << "1st min is: " << first_min <<endl;
        cout << "2nd min is: " << second_min <<endl;
    }

   for(int i = 0; i<11; i++){
    if(arr[11]/2 == 0){      Median law for even = arr[size /2]
      medi = 11/2;
      med = i;
    }
    else{               //Median law for odd = {arr[size/2]+arr[size/2]-1}/2.0

    }

  }*/

}
 }
int main()
{
    float arr[13];

    float arr_size = input_taker(arr);

    cout << arr_size <<endl;
}
